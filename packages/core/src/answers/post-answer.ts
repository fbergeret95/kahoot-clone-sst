import { Answer } from "./types";

export async function postAnswer(answer: Answer): Promise<void> {
  // Calculate time in seconds
  // Insert answer by user
  //   if answer already existed parse db error and throw controlled error
}
